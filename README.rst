dykhne
===========================

Numerical procedure to calculate nonadiabatic transition probabilities using Dykhne's formula.

Installation
----------------------------------

The following installation procedure was tested:

.. code:: shell

        conda create -n dykhne python=3.13
        conda activate dykhne
        python -m pip install -e .
